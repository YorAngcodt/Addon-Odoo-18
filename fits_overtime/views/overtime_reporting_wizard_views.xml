<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Wizard Form View -->
    <record id="view_overtime_reporting_wizard_form" model="ir.ui.view">
        <field name="name">overtime.reporting.wizard.form</field>
        <field name="model">overtime.reporting.wizard</field>
        <field name="arch" type="xml">
            <form string="Overtime Report Filter">
                <group>
                    <group>
                        <field name="employee_ids" widget="many2many_tags"/>
                        <field name="overtime_configuration_id"/>
                    </group>
                    <group>
                        <field name="date_from"/>
                        <field name="date_to"/>
                    </group>
                </group>
                <footer>
                    <button name="action_generate_report" string="Generate Report" type="object" class="btn-primary"/>
                    <button name="action_print_pdf" string="Print PDF" type="object" class="btn-secondary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>
</odoo>