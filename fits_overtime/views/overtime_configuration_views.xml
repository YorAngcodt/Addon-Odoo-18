<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Configuration List View -->
    <record id="view_overtime_configuration_list" model="ir.ui.view">
        <field name="name">overtime.configuration.list</field>
        <field name="model">overtime.configuration</field>
        <field name="arch" type="xml">
            <list string="Overtime Configuration">
                <field name="name"/>
                <field name="period_display"/>
                <field name="status" widget="badge"
                       decoration-success="status == 'active'"
                       decoration-info="status == 'draft'"/>
            </list>
        </field>
    </record>

    <!-- Configuration Form View -->
    <record id="view_overtime_configuration_form" model="ir.ui.view">
        <field name="name">overtime.configuration.form</field>
        <field name="model">overtime.configuration</field>
        <field name="arch" type="xml">
            <form string="Overtime Configuration">
                <header>
                    <button name="action_activate" string="Activate" type="object"
                            class="btn-success"
                            invisible="status != 'draft'"
                            groups="fits_overtime.group_overtime_manager"/>
                    <button name="action_set_draft" string="Set to Draft" type="object"
                            class="btn-secondary"
                            invisible="status == 'draft'"
                            groups="fits_overtime.group_overtime_manager"/>
                    <field name="status" widget="statusbar" statusbar_visible="draft,active"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Configuration Name" readonly="status != 'draft'"/>
                        </h1>
                    </div>
                    <group>
                        <group string="Period">
                            <field name="date_start" readonly="status != 'draft'"/>
                            <field name="date_end" readonly="status != 'draft'"/>
                            <field name="period_display" readonly="1"/>
                        </group>
                        <group string="Statistics">
                            <field name="lines_count" readonly="1"/>
                            <field name="total_overtime_hours" readonly="1"/>
                            <field name="sequence_valid" invisible="1"/>
                            <field name="sequence_status" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="OT Lines" name="ot_lines">
                            <field name="line_ids" readonly="status != 'draft'">
                                <list string="OT Lines" editable="bottom">
                                    <field name="type"/>
                                    <field name="type_week"/>
                                    <field name="start_time" widget="float_time"/>
                                    <field name="end_time" widget="float_time"/>
                                </list>
                            </field>
                        </page>
                        <page string="Summary" name="summary">
                            <group>
                                <div class="alert alert-info" role="alert">
                                    <strong>Configuration Summary</strong><br/>
                                    This configuration defines overtime rules for the specified period.
                                    Only active configurations can be assigned to employees.
                                </div>
                                <group string="Totals">
                                    <field name="total_ot1_hours" readonly="1"/>
                                    <field name="total_ot2_hours" readonly="1"/>
                                    <field name="total_ot3_hours" readonly="1"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Configuration Action -->
    <record id="action_overtime_configuration" model="ir.actions.act_window">
        <field name="name">Overtime Configuration</field>
        <field name="res_model">overtime.configuration</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first overtime configuration!
            </p>
            <p>
                Define overtime rules with OT1, OT2, and OT3 time ranges.
            </p>
        </field>
    </record>
</odoo>